# n8n local com PostgreSQL

Ambiente local do n8n usando Docker Compose e PostgreSQL.

## Requisitos
- Docker
- Docker Compose

## Setup
```bash
cp .env.example .env
docker compose up -d

http://localhost:5678